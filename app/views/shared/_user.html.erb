<div class="d-flex w-50 w-100-mobile mb-3">
  <img src="<%= user.photo %>"/>
  <div class="d-flex flex-column align-items-end justify-content-center w-100 bg-light p-2">
    <h2 class="align-self-start"><%= user.name %></h2>
    <p>Number of posts
      <span class="badge bg-secondary">
        <%= user.posts_counter %>
      <span>
    </p>
  </div>
</div>